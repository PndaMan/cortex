{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities (auto-updater). Excluded on iOS/Android, where the updater plugin isn't compiled — so its permission set must not be required there.",
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "updater:default"
  ]
}
